-- redis_test.lua
local log_info  = logger.info
local serialize = logger.serialize

local DBGroup   = enum("DBGroup")

local timer_mgr = quanta.get("timer_mgr")

local RedisMgr = import("kernel/store/redis_mgr.lua")
local redis_mgr = RedisMgr(DBGroup.AREA)

timer_mgr:once(3000, function()
    local code, res = redis_mgr:execute(3, "get", "aaa")
    log_info("db get code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "set", "aaa", 123)
    log_info("db set code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "get", "aaa")
    log_info("db get code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "del", "aaa")
    log_info("db del code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "get", "aaa")
    log_info("db get code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "hget", "bb", "k1")
    log_info("db hget code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "hset", "bb", "k1", 2)
    log_info("db hset code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "hget", "bb", "k1")
    log_info("db hget code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "hdel", "bb", "k1")
    log_info("db hdel code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "hget", "bb", "k1")
    log_info("db hget code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "subscribe", "test")
    log_info("db subscribe code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "publish", "test", 123)
    log_info("db publish code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "sadd", "set1","one")
    log_info("db sadd code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "scard", "set1")
    log_info("db scard code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "sismember", "set1","one")
    log_info("db sismember code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "sismember", "set1","two")
    log_info("db sismember code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "smembers", "set1")
    log_info("db smembers code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "spop", "set1")
    log_info("db spop code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zadd", "zset1",1,"one")
    log_info("db zadd code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zadd", "zset1",1,"uno")
    log_info("db zadd code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zadd", "zset1",2,"two")
    log_info("db zadd code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zadd", "zset1",3,"three")
    log_info("db zadd code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zcard", "zset1")
    log_info("db zcard code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zcount", "zset1",1,2)
    log_info("db zcount code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zrange", "zset1",0,2)
    log_info("db zrange code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zrank", "zset1","two")
    log_info("db zrank code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zscore", "zset1","two")
    log_info("db zscore code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zrange", "zset1", 0, 2, "WITHSCORES")
    log_info("db zrange withscores code: %s, res = %s", code, serialize(res))
    code, res = redis_mgr:execute(3, "zrangebyscore", "zset1", 0, 2)
    log_info("db zrange zrangebyscore code: %s, res = %s", code, serialize(res))
end)